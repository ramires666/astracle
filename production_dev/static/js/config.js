/**
 * Frontend Config
 *
 * Kept in a tiny module so other files can import it without circular deps.
 */

export const CONFIG = {
    API_BASE: '', // same origin
    DEFAULT_BACKTEST_DAYS: 180,
    DEFAULT_FORECAST_DAYS: 90,
    CHART_ANIMATION_DURATION: 800,
};

